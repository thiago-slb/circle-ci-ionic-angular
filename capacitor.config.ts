import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'circle-ci-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
